{
  "name": "select-component-comparison",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/select-component-comparison/",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.4.2",
    "gh-pages": "^6.0.0",
    "svelte": "^4.0.5",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "choices.js": "^10.2.0",
    "slim-select": "^2.6.0",
    "tom-select": "^2.3.0"
  }
}
